VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# E.164 without +, e.g. 201234567890
VITE_WHATSAPP_NUMBER=
# Admin area (/admin) — single password, no email on the form. Required for sign-in.
VITE_ADMIN_PASSWORD=
# Optional: shown on the admin session (e.g. in tooling); not used for sign-in
VITE_ADMIN_USER_EMAIL=
